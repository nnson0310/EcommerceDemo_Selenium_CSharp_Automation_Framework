namespace EcommerceDemo.enums
{
    internal sealed class EnumCollect
    {
        public enum BrowserName
        {
            Firefox, Chrome, H_Chrome, H_Firefox, Edge, CocCoc, Safari
        }

        public enum Os
        {
            Windows, Linux, Mac
        }

        public enum Environment
        {
            Grid, Local, Cloud
        }
    }
}
